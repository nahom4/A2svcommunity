class Solution:
    def kthLargestNumber(self, nums: List[str], k: int) -> str:
        val = []
        for x in nums:
            val.append(int(x))
        val.sort(reverse=True)
        print(nums)
        return str(val[k-1])
